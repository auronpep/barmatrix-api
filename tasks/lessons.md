# Lessons

- 2026-06-13: Treat `C:\BMO` as the BarMatrix operational map/source context. Its repo folders are junctions into the active `barmatrix-*` repos, and dirty main checkouts should not be touched when an isolated worktree already exists for the launch fix.
